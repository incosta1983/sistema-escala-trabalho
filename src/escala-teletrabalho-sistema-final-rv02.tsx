import ScheduleApp from './scheduleApp/ScheduleApp';

/**
 * Este é o arquivo orquestrador.
 * Ele serve como ponto de entrada para o sistema de agendamento,
 * mantendo a compatibilidade com o resto do projeto.
 * A lógica principal foi refatorada e dividida em módulos
 * menores dentro da pasta /scheduleApp.
 */
export default ScheduleApp;