import React from 'react'
import ReactDOM from 'react-dom/client'
import ScheduleApp from './escala-teletrabalho-sistema-final-rv02.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ScheduleApp />
  </React.StrictMode>,
)